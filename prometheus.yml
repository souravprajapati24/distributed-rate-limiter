global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'rate-limiter'
    static_configs:
      - targets:                         # Development:
          - app:8080  # host.docker.internal:8080
                                         # Production/Docker:
                                        # app:8080

    metrics_path: '/actuator/prometheus'
    scrape_interval: 5s