apiVersion: 1

datasources:
  - name: Prometheus
    uid: prometheus-datasource
    type: prometheus
    access: proxy
    url: http://prometheus:9090   # resolves via the shared rl-network bridge (Phase 1)
    isDefault: true
    editable: false               # provisioned config should not be silently
    # overwritten by a manual UI edit